/**
 * Barrel export del módulo Stripe.
 * Importa desde aquí para mantener rutas cortas:
 *
 *   import StripeCheckoutModal from 'app/shared/components/stripe';
 */

export { default } from './StripeCheckoutModal';
export { default as StripeCheckoutModal } from './StripeCheckoutModal';
export { default as StripeCheckoutForm } from './StripeCheckoutForm';
export type { StripeCheckoutModalProps } from './StripeCheckoutModal';
